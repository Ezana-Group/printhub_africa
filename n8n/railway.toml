[build]
  builder = "DOCKERFILE"

[deploy]
  healthcheckPath = "/healthz"
  healthcheckInterval = 10
  # Increase timeout significantly for n8n's cold start
  healthcheckTimeout = 600
  # Add initial delay to skip immediate checks while initialization occurs
  healthcheckInitialDelay = 45
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
  numReplicas = 1

# Note: healthcheckPort is handled by Railway detecting the dynamic $PORT
